# 🚀 빠른 시작 가이드

컴퓨터를 재시작했거나 나중에 다시 작업할 때 사용하세요.

## ✅ 사전 확인 (최초 1회만)

이미 설치가 완료되었는지 확인:

```powershell
cd d:\03.Code\LightRAG
.\tools\check_installation.ps1
```

**모든 항목이 OK이면** → 바로 아래 "일상 시작" 실행!

---

## 📅 일상 시작 (매번 실행)

### VSCode에서 작업

1. **VSCode 실행**
2. **Open Folder** → `d:\03.Code\LightRAG`
3. **터미널 3개 열기** (`` Ctrl + ` `` 후 분할)

### 터미널 1️⃣: 로컬 임베딩 서버

```powershell
.\tools\run_embedding_server.ps1
```

**실행 상태**: 계속 켜두기 (닫지 말 것)
**출력**:
```
Loading model: paraphrase-multilingual-mpnet-base-v2...
Model loaded. Embedding dimension: 768
INFO:     Uvicorn running on http://127.0.0.1:8001
```

### 터미널 2️⃣: LightRAG 서버

```powershell
.\.venv\Scripts\Activate.ps1
lightrag-server
```

**실행 상태**: 계속 켜두기 (닫지 말 것)
**출력**:
```
INFO:     Uvicorn running on http://0.0.0.0:9621
```

### 터미널 3️⃣: PDF 처리 (필요시)

```powershell
# 예시
.\tools\process_pdf.ps1 "inputs\파일명.pdf"
```

---

## 🌐 웹 UI 접속

브라우저에서: **http://localhost:9621/webui**

---

## 🔍 상태 확인

### 임베딩 서버 확인
```powershell
curl http://127.0.0.1:8001
```
예상 결과: `{"status":"ok","model":"paraphrase-multilingual-mpnet-base-v2","dimension":768}`

### LightRAG 서버 확인
```powershell
curl http://localhost:9621/health
```

### 로그 확인 (실시간)
```powershell
Get-Content logs\lightrag.log -Tail 50 -Wait
```

**성공 신호**: 429 에러 없음!

---

## 🛑 종료 방법

1. 터미널 1, 2에서 `Ctrl + C` 눌러서 서버 중지
2. VSCode 닫기

---

## ⚠️ 문제 해결

### "tesseract를 찾을 수 없습니다" 에러

**새 PowerShell/터미널**을 열어야 PATH가 적용됩니다.

또는 현재 터미널에서:
```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

### 임베딩 서버가 이미 실행 중

```powershell
# 포트 8001 사용 중인 프로세스 확인
Get-NetTCPConnection -LocalPort 8001 -State Listen -ErrorAction SilentlyContinue

# 종료
Stop-Process -Id <PID>
```

### LightRAG 서버가 이미 실행 중

```powershell
# 포트 9621 사용 중인 프로세스 확인
Get-NetTCPConnection -LocalPort 9621 -State Listen -ErrorAction SilentlyContinue

# 종료
Stop-Process -Id <PID>
```

---

## 📚 더 자세한 가이드

- 전체 가이드: `tools\README.md`
- PATH 설정: `tools\PATH_설정_가이드.md`
- 빠른 시작: `tools\QUICKSTART.md`
